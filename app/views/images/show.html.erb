<h1><%= @image.id %></hi>

<img src=<%= "#{@image.img}" %>>

<%#= button_to "Favorite", image_favorite_path(@image), :action => "create", :remote => true, :id => "fav", :hidden => @favorited %>

<%#= button_to "Unfavorite", image_favorite_path(@image), :method => :delete, :remote => true, :hidden => (not @favorited), :id => "unfav" %>


<%= button_to "Favorite", image_favorite_path(@image), :action => "create", :remote => true, :id => "fav", :hidden => @favorited %>


<%= button_to "Unfavorite", image_favorite_path(@image), :method => :delete, :remote => true, :hidden => (not @favorited), :id => "unfav" %>


<script type="text/javascript">
  $(function () {
    //$('.button_to').on('ajax:success', function () {
    $('.button_to').on("click", function () {
      $('#fav, #unfav').toggle();
    });
  });
</script>


